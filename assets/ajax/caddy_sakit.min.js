var $ = jQuery.noConflict();

$(document).ready(function() {
    var table;

    table = $("#table-caddy-sakit").DataTable({
        processing: true, //Feature control the processing indicator.
        serverSide: true, //Feature control DataTables' server-side processing mode.
        order: [], //Initial no order.

        // Load data for the table's content from an Ajax source
        ajax: {
            url: rootWebService + "CaddyMaster/Tampil_Data_Caddy_Sakit",
            type: "POST",
        },

        //Set column definition initialisation properties.
        columnDefs: [{
                targets: [-1], //last column
                orderable: false, //set not orderable
            },
            {
                targets: 0,
                className: "text-center",
            },
            {
                targets: 1,
                className: "text-center",
            },
            {
                targets: 3,
                className: "text-center",
            },
            {
                targets: 4,
                className: "text-center",
            },
            {
                targets: 5,
                className: "text-center",
            },
        ],
    });

    function reload_table() {
        table.ajax.reload(null, false); //reload datatable ajax
    }

    //FUNCTION Hapus Absen
    $("#show_data").on("click", ".hapus-absen", function() {
        var id = $(this).attr("data");
        var data_nama = $(this).attr("data-nama");
        Notiflix.Confirm.Show(
            "Konfrimasi",
            "Anda Yakin ingin menghapus Absen " + data_nama + " ?",
            "Ya",
            "Batal",
            function() {
                $.ajax({
                    type: "POST",
                    url: rootWebService + "CaddyMaster/DeleteAbsenSakit/" + id,
                    dataType: "JSON",
                    success: function(data) {
                        var msg = data.msg;
                        if (msg === "SUCCESS") {
                            Notiflix.Notify.Success("Berhasil menghapus Absen !");
                            reload_table();
                        } else if (msg === "FAIL") {
                            Notiflix.Report.Warning(
                                "Terjadi Kesalahan !",
                                "Tidak dapat terhubung ke database, coba beberapa saat lagi <br><br> - System",
                                "Tutup"
                            );
                        }
                    }, //success
                    error: function(xhr, ajaxOption, thrownError) {
                        Notiflix.Report.Failure(
                            "Terjadi Kesalahan !",
                            "Server tidak merespon permintaan !! - System<br>",
                            "Tutup"
                        );
                    }, //error
                });
            },
            false
        );
    });
});